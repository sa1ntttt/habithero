# URL backend API.
# Оставь ПУСТЫМ для dev (Vite сам проксирует /api/* на localhost:8000).
# Заполни абсолютным URL только если бэкенд живёт на другом домене.
VITE_API_URL=

# Включает dev-режим: подмена initData для разработки в обычном браузере (без Telegram).
VITE_DEV_MODE=true
